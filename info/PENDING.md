Possible things to do
- Make a way to make color schemes (perhaps a .yaml that could pick up variables like "main color" & "secondary color" and include some in the source project)
    - Make a way to have "customizable themes", you could choose the color scheme, certain variants for certain cursors and then the build.sh would look which  "theme.yaml"(s) where available and it'll build from there. 
        a. Two variants for the x-term cursor
        b. Color variants (which are borrowed from the original cursor repo)
        c. Maybe variants for the "busy" cursor
        d. Maybe variants for the "working in the background" cursor
        e. Maybe a variant for the "help" cursor

